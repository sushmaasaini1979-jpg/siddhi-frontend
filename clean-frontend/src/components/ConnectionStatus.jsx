import React from 'react'

const ConnectionStatus = () => {
  // Hide connection status completely to prevent user confusion
  // The socket will handle reconnection silently in the background
  return null
}

export default ConnectionStatus
